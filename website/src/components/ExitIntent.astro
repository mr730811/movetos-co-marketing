---
import { t, getLocalizedPath, joinPath } from '../i18n/utils';
import type { Lang } from '../i18n/utils';

interface Props { lang?: Lang; }
const { lang = 'de' } = Astro.props;
const base = import.meta.env.BASE_URL;

const demoLink = lang === 'en'
  ? getLocalizedPath(joinPath(base, '/ops/demo/'), 'en', base)
  : joinPath(base, '/ops/demo/');
---
<div id="exit-intent-overlay" class="fixed inset-0 z-[100] hidden items-center justify-center bg-black/40 backdrop-blur-sm">
  <div class="glass-card max-w-md mx-4 p-8 text-center relative">
    <button id="exit-intent-close" class="absolute top-4 right-4 w-8 h-8 flex items-center justify-center text-text-muted hover:text-text-primary transition-colors rounded-lg hover:bg-dark-border/30 cursor-pointer">
      &#10005;
    </button>

    <div class="w-12 h-12 rounded-xl bg-brand-orange-glow flex items-center justify-center mb-4 mx-auto">
      <svg class="w-6 h-6 text-brand-orange" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M15.59 14.37a6 6 0 0 1-5.84 7.38v-4.8m5.84-2.58a14.98 14.98 0 0 0 6.16-12.12A14.98 14.98 0 0 0 9.631 8.41m5.96 5.96a14.926 14.926 0 0 1-5.841 2.58m-.119-8.54a6 6 0 0 0-7.381 5.84h4.8m2.581-5.84a14.927 14.927 0 0 0-2.58 5.84m2.699 2.7c-.103.021-.207.041-.311.06a15.09 15.09 0 0 1-2.448-2.448 14.9 14.9 0 0 1 .06-.312m-2.24 2.39a4.493 4.493 0 0 0-1.757 4.306 4.493 4.493 0 0 0 4.306-1.758M16.5 9a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0Z"/></svg>
    </div>
    <h3 class="font-display text-xl font-bold mb-2">{t(lang, 'exitIntent.heading')}</h3>
    <p class="text-sm text-text-secondary leading-relaxed mb-6">
      {t(lang, 'exitIntent.text')}
    </p>

    <a  href={demoLink} class="btn btn-primary w-full justify-center mb-3">
      {t(lang, 'exitIntent.cta')} &rarr;
    </a>
    <button id="exit-intent-dismiss" class="text-xs text-text-muted hover:text-text-secondary transition-colors cursor-pointer bg-transparent border-none">
      {t(lang, 'exitIntent.dismiss')}
    </button>
  </div>
</div>

<script>
  let exitShown = false;

  const show = () => {
    if (exitShown) return;
    if (sessionStorage.getItem('exit-intent-dismissed')) return;
    exitShown = true;
    const overlay = document.getElementById('exit-intent-overlay');
    if (overlay) {
      overlay.classList.remove('hidden');
      overlay.classList.add('flex');
    }
  };

  const hide = () => {
    const overlay = document.getElementById('exit-intent-overlay');
    if (overlay) {
      overlay.classList.add('hidden');
      overlay.classList.remove('flex');
    }
    sessionStorage.setItem('exit-intent-dismissed', 'true');
  };

  document.addEventListener('mouseout', (e) => {
    if (e.clientY <= 0) show();
  });

  document.getElementById('exit-intent-close')?.addEventListener('click', hide);
  document.getElementById('exit-intent-dismiss')?.addEventListener('click', hide);
  document.getElementById('exit-intent-overlay')?.addEventListener('click', (e) => {
    if (e.target === e.currentTarget) hide();
  });
</script>
